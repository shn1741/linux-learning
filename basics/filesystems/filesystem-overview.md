# Filesystem Overview

## Introduction to Filesystems
A filesystem organizes data on a storage device and defines how files and directories are stored and accessed.

On Linux systems, the filesystem is structured as a **tree** starting at the root directory, denoted by `/`.

- All files and directories exist under `/`
- Path elements are separated by forward slashes `/`

Example:
`/home/user/documents`
