# rm command

## What it does
Removes (deletes) files from the filesystem.

## Basic syntax
rm [options] file_name

## Common options / examples
- rm file.txt           # Delete a file
- rm a.txt b.txt        # Delete multiple files

## Notes / mistakes
- Deleted files do not go to a recycle bin
- Use carefully — deletion is permanent
